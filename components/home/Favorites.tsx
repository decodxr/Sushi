import Link from 'next/link';import {ArrowUpRight} from 'lucide-react';import {products} from '@/data/menu';import {money} from '@/lib/format';
export function Favorites(){return <section id="favoritos" className="py-24 md:py-36"><div className="container"><div className="mb-14 flex items-end justify-between border-b rule pb-6"><div><span className="kicker text-signal">Escolhas sem erro • 01—03</span><h2 className="display mt-4 text-5xl md:text-7xl">OS FAVORITOS<br/>DA CASA.</h2></div><span className="hidden text-6xl text-black/10 md:block">注文</span></div><div className="grid gap-px overflow-hidden border rule bg-black/20 md:grid-cols-3">{products.slice(0,3).map((p,i)=><Link href="/cardapio" key={p.id} className="group bg-paper p-5"><div className={`food-visual grain mb-6 aspect-[4/3] ${i===1?'bg-wine':''}`}><span className="absolute left-4 top-4 z-10 bg-paper px-2 py-1 text-[10px] font-black">0{i+1}</span></div><div className="flex items-start justify-between"><div><p className="kicker text-steel">{p.eyebrow}</p><h3 className="display mt-2 text-3xl">{p.name}</h3><p className="mt-3 text-sm font-bold">A partir de {money(p.promotionalPrice??p.price)}</p></div><ArrowUpRight className="transition group-hover:-translate-y-1 group-hover:translate-x-1"/></div></Link>)}</div></div></section>}
