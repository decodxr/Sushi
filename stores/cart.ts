'use client';
import { create } from 'zustand'; import { persist } from 'zustand/middleware'; import type { CartItem } from '@/types';
type State={items:CartItem[];coupon?:string;add:(item:CartItem)=>void;remove:(key:string)=>void;setQty:(key:string,q:number)=>void;edit:(key:string,changes:Partial<Pick<CartItem,'extras'|'removals'|'note'|'quantity'>>)=>void;applyCoupon:(code:string)=>boolean;clear:()=>void};
export const useCart=create<State>()(persist((set,get)=>({items:[],add:item=>set({items:[...get().items,item]}),remove:key=>set({items:get().items.filter(x=>x.key!==key)}),setQty:(key,q)=>set({items:get().items.map(x=>x.key===key?{...x,quantity:Math.max(1,q)}:x)}),edit:(key,changes)=>set({items:get().items.map(item=>item.key===key?{...item,...changes}:item)}),applyCoupon:code=>{const normalized=code.trim().toUpperCase();set({coupon:normalized||undefined});return Boolean(normalized)},clear:()=>set({items:[],coupon:undefined})}),{name:'sushi-em-casa-cart',version:2}));
