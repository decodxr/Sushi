'use client';import {useEffect,useState} from 'react';import Link from 'next/link';export default function Track(){const [order,setOrder]=useState<{order:number}|null>(null);useEffect(()=>{const raw=localStorage.getItem('last-order');if(raw)setOrder(JSON.parse(raw))},[]);return <main className="grid min-h-screen place-items-center bg-paper p-5"><div className="max-w-xl text-center"><p className="kicker text-signal">Tempo real</p><h1 className="display mt-4 text-6xl">CADÊ MEU<br/>SUSHI?</h1>{order?<div className="mt-10 border border-ink p-8 text-left"><b>PEDIDO #{order.order}</b><p className="mt-4 text-signal">● Recebido pelo restaurante</p><p className="mt-2 text-sm text-steel">Acompanhe a confirmação pelo WhatsApp.</p></div>:<><p className="mt-7 text-steel">Nenhum pedido recente neste aparelho.</p><Link href="/cardapio" className="btn btn-dark mt-7">Ir ao cardápio</Link></>}</div></main>}
