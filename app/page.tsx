import {Hero} from '@/components/home/Hero';import {Favorites} from '@/components/home/Favorites';import {HomeSections} from '@/components/home/HomeSections';import {Footer} from '@/components/layout/Footer';import {getMenu} from '@/services/menu';import type {Product} from '@/types';
export const revalidate=60;export default async function Home(){const jsonLd={"@context":"https://schema.org","@type":"Restaurant",name:'Sushi em Casa Express',servesCuisine:'Japanese',address:{"@type":"PostalAddress",streetAddress:'R. Palotina, 227',addressLocality:'Campo Mourão',addressRegion:'PR',postalCode:'87310-606',addressCountry:'BR'}};let featured:Product[]=[];try{const menu=await getMenu();featured=menu.products.filter(p=>p.featured);if(!featured.length)featured=menu.products.slice(0,3)}catch{}return <main id="conteudo"><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(jsonLd)}}/><Hero/><Favorites products={featured}/><HomeSections/><Footer/></main>}
