import {NextResponse} from 'next/server';import {createClient} from '@/lib/supabase/server';import {createOrder} from '@/app/api/orders/route';const sources=new Set(['whatsapp','phone','counter','admin']);export async function POST(request:Request){const db=await createClient();const {data:{user}}=await db.auth.getUser();if(!user)return NextResponse.json({error:'Não autorizado'},{status:401});const {data:profile}=await db.from('admin_profiles').select('id').eq('id',user.id).maybeSingle();if(!profile)return NextResponse.json({error:'Não autorizado'},{status:403});const body=await request.json();if(!sources.has(body.orderSource))return NextResponse.json({error:'Origem inválida'},{status:400});return createOrder(new Request(request.url,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)}),body.orderSource,true)}
