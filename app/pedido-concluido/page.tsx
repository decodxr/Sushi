import Link from 'next/link';export default async function Success({searchParams}:{searchParams:Promise<{pedido?:string}>}){const {pedido='1042'}=await searchParams;return <main className="grid min-h-screen place-items-center bg-ink p-5 text-white"><div className="w-full max-w-2xl border border-white/20 p-8 md:p-14"><p className="kicker text-signal">Pedido #{pedido}</p><h1 className="display mt-5 text-5xl md:text-7xl">RECEBEMOS.<br/>AGORA DEIXA<br/>COM A GENTE. 🍣</h1><ol className="mt-12 border-l border-white/20 pl-7">{['Pedido recebido','Em preparação','Saiu para entrega','Entregue'].map((x,i)=><li className={`relative py-5 ${i?'text-white/35':''}`} key={x}><span className={`absolute -left-[34px] top-6 size-3 rounded-full ${i?'bg-white/20':'bg-signal'}`}/><b>{x}</b></li>)}</ol><Link href="/acompanhar" className="btn btn-primary mt-8">Acompanhar pedido</Link></div></main>}
