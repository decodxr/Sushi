import {AdminShell} from '@/components/admin/AdminShell';import {CrudManager} from '@/components/admin/CrudManager';import {createClient} from '@/lib/supabase/server';export default async function Page(){const {data}=await (await createClient()).from('delivery_zones').select('*').order('name');return <AdminShell title="ENTREGAS" action={false}><CrudManager resource="delivery" initial={data??[]} label="Região" fields={[{key:'name',label:'Bairro ou região'},{key:'fee',label:'Taxa',type:'number'},{key:'minimum_order',label:'Pedido mínimo',type:'number'},{key:'is_active',label:'Ativa',type:'checkbox'}]}/></AdminShell>}
