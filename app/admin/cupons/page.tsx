import {AdminShell} from '@/components/admin/AdminShell';import {CrudManager} from '@/components/admin/CrudManager';import {createClient} from '@/lib/supabase/server';export default async function Page(){const {data}=await (await createClient()).from('coupons').select('*').order('created_at',{ascending:false});return <AdminShell title="CUPONS" action={false}><CrudManager resource="coupons" initial={data??[]} label="Cupom" fields={[{key:'code',label:'Código'},{key:'discount_type',label:'Tipo (percent ou fixed)'},{key:'discount_value',label:'Desconto',type:'number'},{key:'minimum_order',label:'Pedido mínimo',type:'number'},{key:'starts_at',label:'Início',type:'datetime-local'},{key:'ends_at',label:'Fim',type:'datetime-local'},{key:'usage_limit',label:'Limite de usos',type:'number'},{key:'is_active',label:'Ativo',type:'checkbox'}]}/></AdminShell>}
