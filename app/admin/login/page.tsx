'use client';import {useState} from 'react';import {createClient} from '@/lib/supabase/client';
export default function AdminLogin(){const [error,setError]=useState('');const [loading,setLoading]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);const form=new FormData(e.currentTarget);try{const {error}=await createClient().auth.signInWithPassword({email:String(form.get('email')),password:String(form.get('password'))});if(error)throw error;location.href='/admin'}catch{setError('E-mail ou senha inválidos.');setLoading(false)}}return <main className="grid min-h-screen place-items-center bg-ink p-5 text-white"><form onSubmit={submit} className="w-full max-w-sm border border-white/20 p-8"><p className="kicker text-signal">Acesso restrito</p><h1 className="display mt-3 text-4xl">SUSHI ADMIN.</h1><label className="mt-8 block text-xs font-bold uppercase">E-mail<input name="email" type="email" required className="mt-2 h-12 w-full bg-white px-3 text-ink"/></label><label className="mt-4 block text-xs font-bold uppercase">Senha<input name="password" type="password" required className="mt-2 h-12 w-full bg-white px-3 text-ink"/></label>{error&&<p role="alert" className="mt-4 text-sm text-signal">{error}</p>}<button disabled={loading} className="btn btn-primary mt-6 w-full disabled:opacity-50">{loading?'Entrando…':'Entrar'}</button></form></main>}
