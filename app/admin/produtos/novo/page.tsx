import {AdminShell} from '@/components/admin/AdminShell';import {ProductEditor} from '@/components/admin/ProductEditor';import {createClient} from '@/lib/supabase/server';export default async function Page(){const {data}=await (await createClient()).from('categories').select('id,name').eq('is_active',true).order('display_order');return <AdminShell title="NOVO PRODUTO" action={false}><ProductEditor categories={data??[]}/></AdminShell>}
