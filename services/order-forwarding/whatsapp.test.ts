import {describe,expect,it} from 'vitest';import {createWhatsAppUrl,formatWhatsAppOrder} from './whatsapp';import type {ForwardingOrder} from './types';
const base:ForwardingOrder={id:'order-id',number:1052,customer:{name:'Maria Oliveira',phone:'(44) 99999-9999'},fulfillmentType:'delivery',paymentMethod:'pix',address:{postalCode:'87310-606',street:'Rua Palotina',number:'227',district:'Parigot de Souza'},lines:[{quantity:2,name:'Hot Philadelphia',options:['Cream cheese'],notes:'Sem cebolinha'}],subtotal:67.8,deliveryFee:7,discount:0,total:74.8};
describe('encaminhamento de pedido',()=>{it('formata PIX, entrega, itens e valores',()=>{const message=formatWhatsAppOrder(base);expect(message).toContain('Pedido #1052');expect(message).toContain('ENTREGA');expect(message).toContain('2x Hot Philadelphia');expect(message).toContain('+ Cream cheese');expect(message).toContain('Pagamento: PIX');expect(message).toContain('TOTAL: R$ 74,80')});it('inclui troco somente para dinheiro',()=>{expect(formatWhatsAppOrder({...base,paymentMethod:'cash',changeFor:100})).toContain('Troco para: R$ 100,00');expect(formatWhatsAppOrder({...base,paymentMethod:'card_on_delivery'})).not.toContain('Troco para')});it('formata retirada e URL sem expor dados bancários',()=>{const message=formatWhatsAppOrder({...base,fulfillmentType:'pickup',address:undefined});expect(message).toContain('RETIRADA');expect(createWhatsAppUrl('(44) 99999-9999',message)).toMatch(/^https:\/\/wa\.me\/44999999999\?text=/)})});
